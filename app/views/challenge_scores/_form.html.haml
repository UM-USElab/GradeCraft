= semantic_form_for @challenge_score do |f|
  = f.inputs do
    = f.input :score
    = f.input :feedback, :as => :text
    = f.input :team_id, :as => :select, :collection => @teams
    = f.input :challenge_id, :as => :select, :collection => @challenges
    = f.buttons