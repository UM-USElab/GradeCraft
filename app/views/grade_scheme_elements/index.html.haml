%h1 Listing grade_scheme_elements

%table
  %tr
    %th Name
    %th Low range
    %th High range
    %th Letter grade
    %th
    %th
    %th

  - @grade_scheme_elements.each do |grade_scheme_element|
    %tr
      %td= grade_scheme_element.name
      %td= grade_scheme_element.low_range
      %td= grade_scheme_element.high_range
      %td= grade_scheme_element.letter_grade
      %td= link_to 'Show', grade_scheme_element
      %td= link_to 'Edit', edit_grade_scheme_element_path(grade_scheme_element)
      %td= link_to 'Destroy', grade_scheme_element, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Grade scheme element', new_grade_scheme_element_path, {:class => 'btn btn-primary'}
