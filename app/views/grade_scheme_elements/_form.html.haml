= simple_form_for(@grade_scheme_element, :html => {:class => 'form-horizontal' }) do |f|
    = f.input :grade_scheme_id
    = f.input :name
    = f.input :low_range
    = f.input :high_range
    = f.input :letter_grade
    = f.button :submit