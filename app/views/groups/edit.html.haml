- if current_user.is_staff?
  %div.span2= render 'layouts/admin_subnav'

%div.span8
  = render 'form'

%div.span12
  //= link_to 'View', @group, :class => 'btn btn-primary'
  //= link_to 'Back', groups_path, :class => 'btn btn-primary'
