= semantic_form_for @grade_scheme do |f|
  = f.inputs do
    = f.select :assignment, :collection => @assignments
  
  = submit_tag 'Create', :class => 'btn btn-primary'
  or
  = link_to 'Cancel', root_path, :class => 'btn btn-primary'