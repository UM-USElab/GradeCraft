= simple_form_for(@criterium) do |f|

  .form-inputs
    = f.input :name
    = f.input :ruberic_id
    = f.input :description

  .form-actions
    %div.btn-toolbar    
      %div.btn-group
        = f.button :submit, :class => "btn-primary"
      %div.btn-group