- if current_user.is_staff? 
  %div.span2= render 'layouts/admin_subnav'

%div.span8
  %h3= "Edit #{@user.name}"
  - if current_user.is_staff? 
    = render 'form'

  %div.span12
    = link_to 'View', @user, :class => "btn btn-inverse"
    = link_to 'Back', users_path, :class => "btn btn-inverse"

