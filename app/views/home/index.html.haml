%div.hero-unit.span11
  - if current_user
    - if current_course
      %p.span8= raw current_course.homepage_message
      %div.span3.pull-right
        %h3 Course News
        %br
        - current_course.news.each do |news|
          %h5= news.title
          %span= news.created_at
          %br
          %span= raw news.content
    - else
      %p Your account hasn't yet been connected to any courses. Contact us for help!
  - else
    %h1 Welcome to GradeCraft 
    %br
    %p Log in to build your grade