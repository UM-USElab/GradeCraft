= simple_form_for(@course_grade_scheme, :html => {:class => 'form-horizontal' }) do |f|
  = f.input :name
  = f.input :letter_grade
  = f.input :low_range
  = f.input :high_range
  
  .form-actions  
    = f.button :submit, :class => 'btn'
    = link_to 'Cancel', course_grade_schemes_path, :class => 'btn'