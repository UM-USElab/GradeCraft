= simple_form_for(@course_grade_scheme, :html => {:class => 'form-horizontal' }) do |f|
  = f.input :name
  
  .form-actions  
    = f.button :submit, :class => 'btn btn-small'
    = button_to 'Cancel', course_grade_schemes_path, :class => 'btn btn-small'